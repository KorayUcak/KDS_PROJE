<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    html { font-size: 62.5%; }
    
    body {
      font-family: 'Outfit', sans-serif;
      background: linear-gradient(135deg, #0a0a1a 0%, #1a1a2e 50%, #0f0f23 100%);
      color: #fff;
      min-height: 100vh;
    }

    .mono { font-family: 'JetBrains Mono', monospace; }

    /* Header */
    .page-header {
      background: linear-gradient(180deg, rgba(76, 201, 240, 0.1), transparent);
      border-bottom: 1px solid rgba(255, 255, 255, 0.08);
      padding: 1.5rem 3rem;
    }

    .header-content {
      max-width: 1400px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .header-left h1 {
      font-size: 2.2rem;
      font-weight: 700;
      margin-bottom: 0.3rem;
    }

    .header-meta {
      display: flex;
      gap: 1rem;
      align-items: center;
    }

    .meta-badge {
      padding: 0.4rem 1rem;
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 2rem;
      font-size: 1.1rem;
      color: rgba(255, 255, 255, 0.7);
    }

    .back-btn {
      padding: 0.8rem 1.5rem;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 0.75rem;
      color: #fff;
      text-decoration: none;
      font-size: 1.2rem;
      transition: all 0.2s;
    }

    .back-btn:hover { background: rgba(255,255,255,0.1); }

    /* Navigation */
    .nav-bar {
      display: flex;
      gap: 0.5rem;
      padding: 1rem 3rem;
      max-width: 1400px;
      margin: 0 auto;
    }

    .nav-link {
      padding: 0.5rem 1rem;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 2rem;
      color: rgba(255,255,255,0.7);
      text-decoration: none;
      font-size: 1.1rem;
      transition: all 0.2s;
    }

    .nav-link:hover { background: rgba(76, 201, 240, 0.1); border-color: rgba(76, 201, 240, 0.3); color: #4cc9f0; }
    .nav-link.active { background: rgba(76, 201, 240, 0.2); border-color: rgba(76, 201, 240, 0.4); color: #4cc9f0; }

    /* Main Content */
    .main-content {
      max-width: 1400px;
      margin: 0 auto;
      padding: 2rem 3rem 4rem;
    }

    /* Hero Score Card */
    .hero-card {
      display: grid;
      grid-template-columns: 1fr 300px;
      gap: 2rem;
      margin-bottom: 3rem;
    }

    .hero-info {
      background: linear-gradient(135deg, rgba(76, 201, 240, 0.08), rgba(76, 201, 240, 0.02));
      border: 1px solid rgba(76, 201, 240, 0.2);
      border-radius: 1.5rem;
      padding: 2rem;
    }

    .country-title {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 1.5rem;
    }

    .country-flag { font-size: 4rem; }
    .country-name { font-size: 2.8rem; font-weight: 800; }
    .country-region { font-size: 1.3rem; color: rgba(255,255,255,0.5); }

    .quick-stats {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 1rem;
    }

    .quick-stat {
      text-align: center;
      padding: 1rem;
      background: rgba(255,255,255,0.03);
      border-radius: 1rem;
    }

    .quick-stat .value { font-size: 1.8rem; font-weight: 700; color: #4cc9f0; }
    .quick-stat .label { font-size: 1rem; color: rgba(255,255,255,0.5); }

    /* Score Box */
    .score-box {
      background: linear-gradient(135deg, #16213e, #1a1a2e);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 1.5rem;
      padding: 2rem;
      text-align: center;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .score-ring {
      width: 180px;
      height: 180px;
      margin: 0 auto 1.5rem;
      position: relative;
    }

    .score-ring svg { width: 100%; height: 100%; transform: rotate(-90deg); }
    .score-ring circle { fill: none; stroke-width: 12; }
    .score-ring .bg { stroke: rgba(255,255,255,0.1); }
    .score-ring .fill { stroke-linecap: round; }

    .score-num {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-size: 4rem;
      font-weight: 800;
    }

    .score-label { font-size: 1.2rem; color: rgba(255,255,255,0.5); margin-bottom: 1rem; }

    .overall-verdict {
      display: inline-block;
      padding: 0.8rem 2rem;
      border-radius: 2rem;
      font-size: 1.3rem;
      font-weight: 700;
    }

    .verdict-success { background: rgba(0,255,136,0.2); color: #00ff88; }
    .verdict-warning { background: rgba(255,193,7,0.2); color: #ffc107; }
    .verdict-danger { background: rgba(244,67,54,0.2); color: #f44336; }
    .verdict-neutral { background: rgba(76,201,240,0.2); color: #4cc9f0; }

    /* Section Title */
    .section-header {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 2rem;
    }

    .section-header h2 {
      font-size: 2rem;
      font-weight: 700;
    }

    .section-header .subtitle {
      font-size: 1.2rem;
      color: rgba(255,255,255,0.5);
    }

    /* Decision Cards Grid */
    .decisions-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 1.5rem;
    }

    .decisions-grid.full-width {
      grid-template-columns: 1fr;
    }

    /* Decision Card */
    .decision-card {
      background: rgba(255,255,255,0.02);
      border: 2px solid rgba(255,255,255,0.08);
      border-radius: 1.5rem;
      overflow: hidden;
      transition: all 0.3s;
    }

    .decision-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 30px rgba(0,0,0,0.3);
    }

    .decision-card.success { border-color: rgba(0,255,136,0.4); }
    .decision-card.warning { border-color: rgba(255,193,7,0.4); }
    .decision-card.danger { border-color: rgba(244,67,54,0.4); }
    .decision-card.neutral { border-color: rgba(76,201,240,0.4); }

    .decision-header {
      padding: 1.5rem;
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
    }

    .decision-card.success .decision-header { background: linear-gradient(90deg, rgba(0,255,136,0.1), transparent); }
    .decision-card.warning .decision-header { background: linear-gradient(90deg, rgba(255,193,7,0.1), transparent); }
    .decision-card.danger .decision-header { background: linear-gradient(90deg, rgba(244,67,54,0.1), transparent); }
    .decision-card.neutral .decision-header { background: linear-gradient(90deg, rgba(76,201,240,0.1), transparent); }

    .decision-number {
      font-size: 1rem;
      color: rgba(255,255,255,0.4);
      margin-bottom: 0.3rem;
    }

    .decision-question {
      font-size: 1.1rem;
      color: rgba(255,255,255,0.6);
      font-style: italic;
    }

    .decision-verdict-badge {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.6rem 1.2rem;
      border-radius: 2rem;
      font-size: 1.2rem;
      font-weight: 700;
      white-space: nowrap;
    }

    .decision-card.success .decision-verdict-badge { background: rgba(0,255,136,0.2); color: #00ff88; }
    .decision-card.warning .decision-verdict-badge { background: rgba(255,193,7,0.2); color: #ffc107; }
    .decision-card.danger .decision-verdict-badge { background: rgba(244,67,54,0.2); color: #f44336; }
    .decision-card.neutral .decision-verdict-badge { background: rgba(76,201,240,0.2); color: #4cc9f0; }

    .decision-body {
      padding: 1.5rem;
    }

    .decision-title {
      font-size: 1.6rem;
      font-weight: 700;
      margin-bottom: 0.3rem;
    }

    .decision-subtitle {
      font-size: 1.2rem;
      color: rgba(255,255,255,0.5);
      margin-bottom: 1rem;
    }

    .decision-explanation {
      font-size: 1.2rem;
      color: rgba(255,255,255,0.7);
      line-height: 1.6;
      margin-bottom: 1.2rem;
      padding: 1rem;
      background: rgba(255,255,255,0.03);
      border-radius: 0.75rem;
      border-left: 3px solid;
    }

    .decision-card.success .decision-explanation { border-color: #00ff88; }
    .decision-card.warning .decision-explanation { border-color: #ffc107; }
    .decision-card.danger .decision-explanation { border-color: #f44336; }
    .decision-card.neutral .decision-explanation { border-color: #4cc9f0; }

    .decision-action {
      display: flex;
      align-items: flex-start;
      gap: 0.8rem;
      padding: 1rem;
      background: rgba(255,255,255,0.02);
      border-radius: 0.75rem;
    }

    .action-icon { font-size: 1.5rem; }
    .action-text { font-size: 1.1rem; color: rgba(255,255,255,0.8); }
    .action-label { font-size: 1rem; color: rgba(255,255,255,0.5); display: block; margin-bottom: 0.2rem; }

    .confidence-bar {
      margin-top: 1rem;
      display: flex;
      align-items: center;
      gap: 0.8rem;
    }

    .confidence-label { font-size: 1rem; color: rgba(255,255,255,0.5); }
    .confidence-track {
      flex: 1;
      height: 6px;
      background: rgba(255,255,255,0.1);
      border-radius: 3px;
      overflow: hidden;
    }
    .confidence-fill {
      height: 100%;
      border-radius: 3px;
      transition: width 0.5s ease;
    }
    .confidence-value { font-size: 1.1rem; font-weight: 600; }

    /* Sticky Action Footer */
    .sticky-footer {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      background: linear-gradient(180deg, rgba(26, 26, 46, 0.95), rgba(22, 33, 62, 0.98));
      border-top: 1px solid rgba(76, 201, 240, 0.3);
      padding: 1rem 3rem;
      z-index: 1000;
      backdrop-filter: blur(10px);
    }

    .footer-content {
      max-width: 1400px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .footer-info {
      display: flex;
      align-items: center;
      gap: 1.5rem;
    }

    .footer-country { font-size: 1.4rem; font-weight: 600; }
    .footer-score {
      background: rgba(76, 201, 240, 0.2);
      color: #4cc9f0;
      padding: 0.4rem 1rem;
      border-radius: 2rem;
      font-size: 1.2rem;
      font-weight: 600;
    }

    .footer-btn {
      padding: 1rem 2.5rem;
      background: linear-gradient(135deg, #4cc9f0, #00ff88);
      border: none;
      border-radius: 2rem;
      color: #000;
      font-size: 1.3rem;
      font-weight: 700;
      cursor: pointer;
      transition: all 0.3s;
      font-family: inherit;
    }

    .footer-btn:hover {
      transform: scale(1.05);
      box-shadow: 0 4px 20px rgba(76, 201, 240, 0.4);
    }

    /* Add padding for sticky footer */
    body { padding-bottom: 8rem; }

    /* Responsive */
    @media (max-width: 1000px) {
      .hero-card { grid-template-columns: 1fr; }
      .decisions-grid { grid-template-columns: 1fr; }
      .quick-stats { grid-template-columns: repeat(2, 1fr); }
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header class="page-header">
    <div class="header-content">
      <div class="header-left">
        <h1>🎯 Executive Verdicts</h1>
        <div class="header-meta">
          <span class="meta-badge">📊 <%= sector.sektor_adi %></span>
          <span class="meta-badge">🏳️ <%= country.ulke_adi %></span>
        </div>
      </div>
      <a href="/sector/<%= sector.sektor_id %>/rankings" class="back-btn">← Sıralamaya Dön</a>
    </div>
  </header>

  <!-- Navigation -->
  <nav class="nav-bar">
    <a href="/" class="nav-link">🗺️ Harita</a>
    <a href="/scenario-builder" class="nav-link">🔬 Senaryo</a>
    <a href="/sector/<%= sector.sektor_id %>/rankings" class="nav-link">📊 Sıralama</a>
    <a href="/sector/<%= sector.sektor_id %>/country/<%= country.ulke_id %>" class="nav-link active">🎯 Kararlar</a>
    <a href="/sector/<%= sector.sektor_id %>/country/<%= country.ulke_id %>/strategy" class="nav-link">🧙 Wizard</a>
    <a href="/decisions" class="nav-link">📋 Kayıtlı Kararlar</a>
  </nav>

  <main class="main-content">
    <!-- Hero Card -->
    <div class="hero-card">
      <div class="hero-info">
        <div class="country-title">
          <span class="country-flag">🏳️</span>
          <div>
            <h1 class="country-name"><%= country.ulke_adi %></h1>
            <p class="country-region"><%= country.bolge_adi || 'N/A' %> • <%= country.ISO_KODU %></p>
          </div>
        </div>
        <div class="quick-stats">
          <div class="quick-stat">
            <div class="value mono">$<%= country.gsyh_kisi_basi_usd ? Math.round(parseFloat(country.gsyh_kisi_basi_usd)).toLocaleString() : 'N/A' %></div>
            <div class="label">Kişi Başı GSYİH</div>
          </div>
          <div class="quick-stat">
            <div class="value mono"><%= country.nufus_milyon ? parseFloat(country.nufus_milyon).toFixed(1) : 'N/A' %>M</div>
            <div class="label">Nüfus</div>
          </div>
          <div class="quick-stat">
            <div class="value"><%= country.risk_notu_kodu || 'N/A' %></div>
            <div class="label">Risk Notu</div>
          </div>
          <div class="quick-stat">
            <div class="value mono"><%= country.lpi_skoru ? parseFloat(country.lpi_skoru).toFixed(2) : 'N/A' %></div>
            <div class="label">LPI Skoru</div>
          </div>
        </div>
      </div>

      <% if (countryRank) { %>
      <div class="score-box">
        <div class="score-ring">
          <svg viewBox="0 0 100 100">
            <circle class="bg" cx="50" cy="50" r="42"/>
            <circle class="fill" cx="50" cy="50" r="42"
              stroke="<%= countryRank.totalScore >= 65 ? '#00ff88' : countryRank.totalScore >= 50 ? '#ffc107' : '#f44336' %>"
              stroke-dasharray="264"
              stroke-dashoffset="<%= 264 * (1 - countryRank.totalScore / 100) %>"
            />
          </svg>
          <div class="score-num mono" style="color: <%= countryRank.totalScore >= 65 ? '#00ff88' : countryRank.totalScore >= 50 ? '#ffc107' : '#f44336' %>">
            <%= countryRank.totalScore %>
          </div>
        </div>
        <div class="score-label">Toplam Uygunluk Skoru</div>
        <span class="overall-verdict verdict-<%= countryRank.totalScore >= 65 ? 'success' : countryRank.totalScore >= 50 ? 'warning' : 'danger' %>">
          <%= countryRank.totalScore >= 65 ? '✅ Önerilen Pazar' : countryRank.totalScore >= 50 ? '⚠️ Potansiyel Var' : '❌ Riskli Pazar' %>
        </span>
      </div>
      <% } %>
    </div>

    <!-- 7 Decisions Section -->
    <div class="section-header">
      <h2>📋 7 Stratejik Karar</h2>
      <span class="subtitle">Veriye dayalı yönetici tavsiyeleri</span>
    </div>

    <% if (decisions) { %>
    <div class="decisions-grid">
      
      <!-- Decision 1: Market Entry -->
      <div class="decision-card <%= decisions.marketEntry.status %>">
        <div class="decision-header">
          <div>
            <div class="decision-number">KARAR #1</div>
            <div class="decision-question">"Bu pazara girmeli miyiz?"</div>
          </div>
          <div class="decision-verdict-badge">
            <%= decisions.marketEntry.icon %> <%= decisions.marketEntry.verdict %>
          </div>
        </div>
        <div class="decision-body">
          <h3 class="decision-title"><%= decisions.marketEntry.title %></h3>
          <p class="decision-subtitle"><%= decisions.marketEntry.subtitle %></p>
          <div class="decision-explanation"><%= decisions.marketEntry.explanation %></div>
          <div class="decision-action">
            <span class="action-icon">💡</span>
            <div>
              <span class="action-label">Önerilen Aksiyon:</span>
              <span class="action-text"><%= decisions.marketEntry.action %></span>
            </div>
          </div>
          <div class="confidence-bar">
            <span class="confidence-label">Güven:</span>
            <div class="confidence-track">
              <div class="confidence-fill" style="width: <%= decisions.marketEntry.confidence %>%; background: <%= decisions.marketEntry.status === 'success' ? '#00ff88' : decisions.marketEntry.status === 'warning' ? '#ffc107' : decisions.marketEntry.status === 'danger' ? '#f44336' : '#4cc9f0' %>;"></div>
            </div>
            <span class="confidence-value mono">%<%= decisions.marketEntry.confidence %></span>
          </div>
        </div>
      </div>

      <!-- Decision 2: Pricing -->
      <div class="decision-card <%= decisions.pricing.status %>">
        <div class="decision-header">
          <div>
            <div class="decision-number">KARAR #2</div>
            <div class="decision-question">"Nasıl fiyatlandırmalıyız?"</div>
          </div>
          <div class="decision-verdict-badge">
            <%= decisions.pricing.icon %> <%= decisions.pricing.verdict %>
          </div>
        </div>
        <div class="decision-body">
          <h3 class="decision-title"><%= decisions.pricing.title %></h3>
          <p class="decision-subtitle"><%= decisions.pricing.subtitle %></p>
          <div class="decision-explanation"><%= decisions.pricing.explanation %></div>
          <div class="decision-action">
            <span class="action-icon">💡</span>
            <div>
              <span class="action-label">Önerilen Aksiyon:</span>
              <span class="action-text"><%= decisions.pricing.action %></span>
            </div>
          </div>
          <div class="confidence-bar">
            <span class="confidence-label">Güven:</span>
            <div class="confidence-track">
              <div class="confidence-fill" style="width: <%= decisions.pricing.confidence %>%; background: <%= decisions.pricing.status === 'success' ? '#00ff88' : decisions.pricing.status === 'warning' ? '#ffc107' : '#4cc9f0' %>;"></div>
            </div>
            <span class="confidence-value mono">%<%= decisions.pricing.confidence %></span>
          </div>
        </div>
      </div>

      <!-- Decision 3: Logistics -->
      <div class="decision-card <%= decisions.logistics.status %>">
        <div class="decision-header">
          <div>
            <div class="decision-number">KARAR #3</div>
            <div class="decision-question">"Nasıl gönderelim?"</div>
          </div>
          <div class="decision-verdict-badge">
            <%= decisions.logistics.icon %> <%= decisions.logistics.verdict %>
          </div>
        </div>
        <div class="decision-body">
          <h3 class="decision-title"><%= decisions.logistics.title %></h3>
          <p class="decision-subtitle"><%= decisions.logistics.subtitle %></p>
          <div class="decision-explanation"><%= decisions.logistics.explanation %></div>
          <div class="decision-action">
            <span class="action-icon">💡</span>
            <div>
              <span class="action-label">Önerilen Mod:</span>
              <span class="action-text"><%= decisions.logistics.recommendedMode %> | Tampon Stok: <%= decisions.logistics.bufferStock %></span>
            </div>
          </div>
          <div class="confidence-bar">
            <span class="confidence-label">Güven:</span>
            <div class="confidence-track">
              <div class="confidence-fill" style="width: <%= decisions.logistics.confidence %>%; background: <%= decisions.logistics.status === 'success' ? '#00ff88' : decisions.logistics.status === 'warning' ? '#ffc107' : '#4cc9f0' %>;"></div>
            </div>
            <span class="confidence-value mono">%<%= decisions.logistics.confidence %></span>
          </div>
        </div>
      </div>

      <!-- Decision 4: Financial -->
      <div class="decision-card <%= decisions.financial.status %>">
        <div class="decision-header">
          <div>
            <div class="decision-number">KARAR #4</div>
            <div class="decision-question">"Paramız güvende mi?"</div>
          </div>
          <div class="decision-verdict-badge">
            <%= decisions.financial.icon %> <%= decisions.financial.verdict %>
          </div>
        </div>
        <div class="decision-body">
          <h3 class="decision-title"><%= decisions.financial.title %></h3>
          <p class="decision-subtitle"><%= decisions.financial.subtitle %></p>
          <div class="decision-explanation"><%= decisions.financial.explanation %></div>
          <div class="decision-action">
            <span class="action-icon">💡</span>
            <div>
              <span class="action-label">Para Birimi Önerisi:</span>
              <span class="action-text"><%= decisions.financial.currencyRecommendation %></span>
            </div>
          </div>
          <div class="confidence-bar">
            <span class="confidence-label">Güven:</span>
            <div class="confidence-track">
              <div class="confidence-fill" style="width: <%= decisions.financial.confidence %>%; background: <%= decisions.financial.status === 'success' ? '#00ff88' : decisions.financial.status === 'warning' ? '#ffc107' : decisions.financial.status === 'danger' ? '#f44336' : '#4cc9f0' %>;"></div>
            </div>
            <span class="confidence-value mono">%<%= decisions.financial.confidence %></span>
          </div>
        </div>
      </div>

      <!-- Decision 5: Marketing -->
      <div class="decision-card <%= decisions.marketing.status %>">
        <div class="decision-header">
          <div>
            <div class="decision-number">KARAR #5</div>
            <div class="decision-question">"Hedef kitlemiz kim?"</div>
          </div>
          <div class="decision-verdict-badge">
            <%= decisions.marketing.icon %> <%= decisions.marketing.verdict %>
          </div>
        </div>
        <div class="decision-body">
          <h3 class="decision-title"><%= decisions.marketing.title %></h3>
          <p class="decision-subtitle"><%= decisions.marketing.subtitle %></p>
          <div class="decision-explanation"><%= decisions.marketing.explanation %></div>
          <div class="decision-action">
            <span class="action-icon">💡</span>
            <div>
              <span class="action-label">Kampanya Odağı:</span>
              <span class="action-text"><%= decisions.marketing.campaignFocus %></span>
            </div>
          </div>
          <div class="confidence-bar">
            <span class="confidence-label">Güven:</span>
            <div class="confidence-track">
              <div class="confidence-fill" style="width: <%= decisions.marketing.confidence %>%; background: <%= decisions.marketing.status === 'success' ? '#00ff88' : decisions.marketing.status === 'warning' ? '#ffc107' : '#4cc9f0' %>;"></div>
            </div>
            <span class="confidence-value mono">%<%= decisions.marketing.confidence %></span>
          </div>
        </div>
      </div>

      <!-- Decision 6: Trade Barrier -->
      <div class="decision-card <%= decisions.tradeBarrier.status %>">
        <div class="decision-header">
          <div>
            <div class="decision-number">KARAR #6</div>
            <div class="decision-question">"Ticari avantajımız var mı?"</div>
          </div>
          <div class="decision-verdict-badge">
            <%= decisions.tradeBarrier.icon %> <%= decisions.tradeBarrier.verdict %>
          </div>
        </div>
        <div class="decision-body">
          <h3 class="decision-title"><%= decisions.tradeBarrier.title %></h3>
          <p class="decision-subtitle"><%= decisions.tradeBarrier.subtitle %></p>
          <div class="decision-explanation"><%= decisions.tradeBarrier.explanation %></div>
          <div class="decision-action">
            <span class="action-icon">💡</span>
            <div>
              <span class="action-label">Tarife Avantajı:</span>
              <span class="action-text"><%= decisions.tradeBarrier.tariffAdvantage %></span>
            </div>
          </div>
          <div class="confidence-bar">
            <span class="confidence-label">Güven:</span>
            <div class="confidence-track">
              <div class="confidence-fill" style="width: <%= decisions.tradeBarrier.confidence %>%; background: <%= decisions.tradeBarrier.status === 'success' ? '#00ff88' : decisions.tradeBarrier.status === 'warning' ? '#ffc107' : '#4cc9f0' %>;"></div>
            </div>
            <span class="confidence-value mono">%<%= decisions.tradeBarrier.confidence %></span>
          </div>
        </div>
      </div>

    </div>

    <!-- Decision 7: Investment Horizon (Full Width) -->
    <div class="decisions-grid full-width" style="margin-top: 1.5rem;">
      <div class="decision-card <%= decisions.investmentHorizon.status %>">
        <div class="decision-header">
          <div>
            <div class="decision-number">KARAR #7</div>
            <div class="decision-question">"Kısa vadeli mi, uzun vadeli mi?"</div>
          </div>
          <div class="decision-verdict-badge">
            <%= decisions.investmentHorizon.icon %> <%= decisions.investmentHorizon.verdict %>
          </div>
        </div>
        <div class="decision-body">
          <h3 class="decision-title"><%= decisions.investmentHorizon.title %></h3>
          <p class="decision-subtitle"><%= decisions.investmentHorizon.subtitle %></p>
          <div class="decision-explanation"><%= decisions.investmentHorizon.explanation %></div>
          <div class="decision-action">
            <span class="action-icon">💡</span>
            <div>
              <span class="action-label">Yatırım Ufku:</span>
              <span class="action-text"><%= decisions.investmentHorizon.horizon %> | Yatırım Seviyesi: <%= decisions.investmentHorizon.investmentLevel %></span>
            </div>
          </div>
          <div class="confidence-bar">
            <span class="confidence-label">Güven:</span>
            <div class="confidence-track">
              <div class="confidence-fill" style="width: <%= decisions.investmentHorizon.confidence %>%; background: <%= decisions.investmentHorizon.status === 'success' ? '#00ff88' : decisions.investmentHorizon.status === 'warning' ? '#ffc107' : '#4cc9f0' %>;"></div>
            </div>
            <span class="confidence-value mono">%<%= decisions.investmentHorizon.confidence %></span>
          </div>
        </div>
      </div>
    </div>
    <% } %>
  </main>

  <!-- Sticky Action Footer -->
  <div class="sticky-footer">
    <div class="footer-content">
      <div class="footer-info">
        <span class="footer-country">🏳️ <%= country.ulke_adi %></span>
        <span class="footer-score"><%= countryRank ? countryRank.totalScore : 0 %> puan</span>
        <% if (decisions && decisions.marketEntry) { %>
          <span class="overall-verdict verdict-<%= decisions.marketEntry.status %>" style="font-size: 1.1rem;">
            <%= decisions.marketEntry.icon %> <%= decisions.marketEntry.verdict %>
          </span>
        <% } %>
      </div>
      <a href="/sector/<%= sector.sektor_id %>/country/<%= country.ulke_id %>/strategy" class="footer-btn" style="margin-right: 12px; background: linear-gradient(135deg, #7c3aed, #a855f7); text-decoration: none;">
        🧙 Strateji Wizard
      </a>
      <button class="footer-btn" onclick="openDecisionPanel({
        ulke_id: '<%= country.ulke_id %>',
        ulke_adi: '<%= country.ulke_adi %>',
        sektor_id: '<%= sector.sektor_id %>',
        sektor_adi: '<%= sector.sektor_adi %>',
        skor: <%= countryRank ? countryRank.totalScore : 0 %>,
        flag: '🏳️'
      })">
        🎯 Hızlı Karar Al
      </button>
    </div>
  </div>

  <!-- Decision Panel Include -->
  <%- include('../includes/decision-panel') %>
</body>
</html>
