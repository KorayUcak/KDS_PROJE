<%- include('../includes/dashboard-header') %>

<div class="dashboard-container">
  <!-- Sayfa Başlığı ve Breadcrumb -->
  <div class="page-header-extended">
    <div class="header-content">
      <div class="breadcrumb-nav">
        <a href="/analyses/dashboard" class="breadcrumb-link">📊 Genel Bakış</a>
        <span class="breadcrumb-separator">›</span>
        <span class="breadcrumb-current">Analiz Detayı</span>
      </div>
      <h1>🎯 Analiz Detayı</h1>
      <p class="page-subtitle">Pazar analizi kapsamlı bilgileri ve değerlendirmeler</p>
    </div>
    <% if (analysis) { %>
    <div class="header-actions">
      <span class="status-badge-large <%= analysis.hesaplanan_skor ? 'status-tamamlandi' : 'status-bekliyor' %>">
        <%= analysis.hesaplanan_skor ? '✅ Tamamlandı' : '⏳ İşlemde' %>
      </span>
    </div>
    <% } %>
  </div>

  <% if (analysis) { %>
  <!-- Özet Kartlar -->
  <div class="summary-cards">
    <div class="summary-card card-country">
      <div class="summary-icon">🌍</div>
      <div class="summary-content">
        <div class="summary-label">Hedef Ülke</div>
        <div class="summary-value"><%= analysis.ulke_adi || 'Bilinmiyor' %></div>
        <% if (analysis.ulke_kodu) { %>
        <div class="summary-meta">ISO: <%= analysis.ulke_kodu %></div>
        <% } %>
      </div>
    </div>
    
    <div class="summary-card card-sector">
      <div class="summary-icon">🏭</div>
      <div class="summary-content">
        <div class="summary-label">Hedef Sektör</div>
        <div class="summary-value"><%= analysis.sektor_adi || 'Bilinmiyor' %></div>
      </div>
    </div>
    
    <% if (analysis.hesaplanan_skor) { %>
    <div class="summary-card card-score">
      <div class="summary-icon">⭐</div>
      <div class="summary-content">
        <div class="summary-label">Pazar Skoru</div>
        <div class="summary-value score-display"><%= parseFloat(analysis.hesaplanan_skor).toFixed(2) %></div>
        <div class="summary-meta">100 üzerinden</div>
      </div>
    </div>
    <% } %>
    
    <div class="summary-card card-date">
      <div class="summary-icon">📅</div>
      <div class="summary-content">
        <div class="summary-label">Oluşturulma</div>
        <div class="summary-value-date">
          <%= analysis.olusturulma_tarihi ? new Date(analysis.olusturulma_tarihi).toLocaleDateString('tr-TR', { 
            day: 'numeric',
            month: 'short',
            year: 'numeric'
          }) : '-' %>
        </div>
        <div class="summary-meta">
          <%= analysis.olusturulma_tarihi ? new Date(analysis.olusturulma_tarihi).toLocaleTimeString('tr-TR', { 
            hour: '2-digit',
            minute: '2-digit'
          }) : '' %>
        </div>
      </div>
    </div>
  </div>

  <!-- Ana Bilgiler Kartı -->
  <div class="detail-card">
    <div class="card-header">
      <div class="card-header-content">
        <h2>📋 Genel Bilgiler</h2>
        <p class="card-subtitle">Analiz temel bilgileri ve parametreleri</p>
      </div>
    </div>
    <div class="card-body">
      <div class="info-grid">
        <div class="info-item">
          <div class="info-icon">🆔</div>
          <div class="info-content">
            <span class="info-label">Analiz ID</span>
            <span class="info-value">#<%= analysis.analiz_id %></span>
          </div>
        </div>
        <div class="info-item">
          <div class="info-icon">🌍</div>
          <div class="info-content">
            <span class="info-label">Hedef Ülke</span>
            <span class="info-value"><%= analysis.ulke_adi || 'Bilinmiyor' %> <% if (analysis.ulke_kodu) { %>(<%= analysis.ulke_kodu %>)<% } %></span>
          </div>
        </div>
        <div class="info-item">
          <div class="info-icon">🏭</div>
          <div class="info-content">
            <span class="info-label">Hedef Sektör</span>
            <span class="info-value"><%= analysis.sektor_adi || 'Bilinmiyor' %></span>
          </div>
        </div>
        <% if (analysis.olusturan_kullanici) { %>
        <div class="info-item">
          <div class="info-icon">👤</div>
          <div class="info-content">
            <span class="info-label">Oluşturan Kullanıcı</span>
            <span class="info-value"><%= analysis.olusturan_kullanici %></span>
          </div>
        </div>
        <% } %>
        <div class="info-item">
          <div class="info-icon">📅</div>
          <div class="info-content">
            <span class="info-label">Oluşturulma Tarihi</span>
            <span class="info-value">
              <%= analysis.olusturulma_tarihi ? new Date(analysis.olusturulma_tarihi).toLocaleDateString('tr-TR', { 
                year: 'numeric', 
                month: 'long', 
                day: 'numeric',
                hour: '2-digit',
                minute: '2-digit'
              }) : '-' %>
            </span>
          </div>
        </div>
        <% if (analysis.hesaplanan_skor) { %>
        <div class="info-item">
          <div class="info-icon">⭐</div>
          <div class="info-content">
            <span class="info-label">Hesaplanan Skor</span>
            <span class="info-value score-value"><%= parseFloat(analysis.hesaplanan_skor).toFixed(2) %> / 100</span>
          </div>
        </div>
        <% } %>
      </div>
    </div>
  </div>

  <!-- İki Kolonlu Düzen: Açıklama ve Notlar -->
  <div class="two-column-layout">
    <!-- Açıklama Kartı -->
    <div class="detail-card <%= !analysis.aciklama ? 'empty-card' : '' %>">
      <div class="card-header">
        <div class="card-header-content">
          <h2>📝 Açıklama</h2>
          <p class="card-subtitle">Analiz açıklaması ve detayları</p>
        </div>
      </div>
      <div class="card-body">
        <% if (analysis.aciklama) { %>
        <div class="description-content">
          <%= analysis.aciklama %>
        </div>
        <% } else { %>
        <div class="empty-content">
          <div class="empty-icon">📄</div>
          <p>Açıklama eklenmemiş</p>
        </div>
        <% } %>
      </div>
    </div>

    <!-- Yönetici Notu -->
    <div class="detail-card <%= !analysis.yonetici_notu ? 'empty-card' : '' %>">
      <div class="card-header">
        <div class="card-header-content">
          <h2>💼 Yönetici Notu</h2>
          <p class="card-subtitle">Yönetici değerlendirmesi</p>
        </div>
      </div>
      <div class="card-body">
        <% if (analysis.yonetici_notu) { %>
        <div class="note-content">
          <%= analysis.yonetici_notu %>
        </div>
        <% } else { %>
        <div class="empty-content">
          <div class="empty-icon">📋</div>
          <p>Yönetici notu eklenmemiş</p>
        </div>
        <% } %>
      </div>
    </div>
  </div>

  <!-- Analiz Durumu ve İstatistikler -->
  <div class="detail-card">
    <div class="card-header">
      <div class="card-header-content">
        <h2>📊 Analiz Durumu</h2>
        <p class="card-subtitle">Mevcut durum ve değerlendirme</p>
      </div>
    </div>
    <div class="card-body">
      <div class="status-grid">
        <div class="status-item">
          <div class="status-indicator <%= analysis.hesaplanan_skor ? 'status-complete' : 'status-pending' %>"></div>
          <div class="status-content">
            <div class="status-title">Analiz Durumu</div>
            <div class="status-value"><%= analysis.hesaplanan_skor ? 'Tamamlandı' : 'İşlemde' %></div>
          </div>
        </div>
        <div class="status-item">
          <div class="status-indicator <%= analysis.aciklama ? 'status-complete' : 'status-warning' %>"></div>
          <div class="status-content">
            <div class="status-title">Açıklama</div>
            <div class="status-value"><%= analysis.aciklama ? 'Mevcut' : 'Eksik' %></div>
          </div>
        </div>
        <div class="status-item">
          <div class="status-indicator <%= analysis.yonetici_notu ? 'status-complete' : 'status-warning' %>"></div>
          <div class="status-content">
            <div class="status-title">Yönetici Notu</div>
            <div class="status-value"><%= analysis.yonetici_notu ? 'Mevcut' : 'Eksik' %></div>
          </div>
        </div>
        <div class="status-item">
          <div class="status-indicator status-complete"></div>
          <div class="status-content">
            <div class="status-title">Veri Bütünlüğü</div>
            <div class="status-value">Tam</div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Aksiyonlar -->
  <div class="action-bar">
    <div class="action-left">
      <a href="/analyses/dashboard" class="btn btn-secondary">
        <span class="btn-icon">←</span>
        <span class="btn-text">Genel Bakış'a Dön</span>
      </a>
    </div>
    <div class="action-right">
      <button class="btn btn-warning" onclick="editAnalysis(<%= analysis.analiz_id %>)" title="Düzenle">
        <span class="btn-icon">✏️</span>
        <span class="btn-text">Düzenle</span>
      </button>
      <button class="btn btn-danger" onclick="deleteAnalysis(<%= analysis.analiz_id %>)" title="Sil">
        <span class="btn-icon">🗑️</span>
        <span class="btn-text">Sil</span>
      </button>
    </div>
  </div>

  <% } else { %>
  <div class="empty-state-full">
    <div class="empty-state-content">
      <div class="empty-icon-large">❌</div>
      <h2>Analiz Bulunamadı</h2>
      <p>Bu analiz mevcut değil, silinmiş olabilir veya erişim yetkiniz bulunmamaktadır.</p>
      <div class="empty-actions">
        <a href="/analyses/dashboard" class="btn btn-primary">
          <span class="btn-icon">←</span>
          <span class="btn-text">Genel Bakış'a Dön</span>
        </a>
        <a href="/analyses/new" class="btn btn-secondary">
          <span class="btn-icon">➕</span>
          <span class="btn-text">Yeni Analiz Oluştur</span>
        </a>
      </div>
    </div>
  </div>
  <% } %>
</div>

<style>
  .page-header-extended {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 3rem 2rem;
    border-radius: 12px;
    margin-bottom: 3rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
  }

  .header-content h1 {
    margin: 0.5rem 0;
    font-size: 2.8rem;
  }

  .breadcrumb-nav {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    font-size: 1.3rem;
    margin-bottom: 1rem;
    opacity: 0.9;
  }

  .breadcrumb-link {
    color: white;
    text-decoration: none;
    transition: opacity 0.2s;
  }

  .breadcrumb-link:hover {
    opacity: 0.8;
  }

  .breadcrumb-separator {
    opacity: 0.6;
  }

  .breadcrumb-current {
    font-weight: 600;
  }

  .status-badge-large {
    padding: 1rem 2rem;
    border-radius: 25px;
    font-size: 1.5rem;
    font-weight: 700;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .status-tamamlandi {
    background: #c6f6d5;
    color: #22543d;
  }

  .status-bekliyor {
    background: #fef3c7;
    color: #78350f;
  }

  .summary-cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .summary-card {
    background: white;
    border-radius: 12px;
    padding: 2rem;
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
    box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .summary-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 4px 15px rgba(0,0,0,0.12);
  }

  .summary-icon {
    font-size: 3rem;
    flex-shrink: 0;
  }

  .summary-content {
    flex: 1;
  }

  .summary-label {
    font-size: 1.2rem;
    color: #718096;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 0.5rem;
  }

  .summary-value {
    font-size: 1.8rem;
    color: #2d3748;
    font-weight: 700;
    margin-bottom: 0.3rem;
  }

  .summary-value-date {
    font-size: 1.6rem;
    color: #2d3748;
    font-weight: 700;
    margin-bottom: 0.3rem;
  }

  .summary-meta {
    font-size: 1.2rem;
    color: #a0aec0;
  }

  .score-display {
    color: #48bb78;
    font-size: 2.2rem;
  }

  .card-country { border-left: 4px solid #3b82f6; }
  .card-sector { border-left: 4px solid #8b5cf6; }
  .card-score { border-left: 4px solid #10b981; }
  .card-date { border-left: 4px solid #f59e0b; }

  .detail-card {
    background: white;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.08);
    margin-bottom: 2rem;
    overflow: hidden;
  }

  .detail-card .card-header {
    background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
    border-bottom: 2px solid #e2e8f0;
    padding: 2rem;
  }

  .card-header-content h2 {
    margin: 0;
    font-size: 2rem;
    color: #2d3748;
    font-weight: 700;
  }

  .card-subtitle {
    margin: 0.5rem 0 0 0;
    font-size: 1.3rem;
    color: #718096;
  }

  .detail-card .card-body {
    padding: 2.5rem;
  }

  .info-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2.5rem;
  }

  .info-item {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
  }

  .info-icon {
    font-size: 2.5rem;
    flex-shrink: 0;
  }

  .info-content {
    flex: 1;
  }

  .info-label {
    display: block;
    font-size: 1.2rem;
    color: #718096;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 0.5rem;
  }

  .info-value {
    font-size: 1.6rem;
    color: #2d3748;
    font-weight: 600;
  }

  .score-value {
    color: #10b981;
    font-size: 1.8rem;
    font-weight: 700;
  }

  .two-column-layout {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: 2rem;
    margin-bottom: 2rem;
  }

  .description-content,
  .note-content {
    font-size: 1.5rem;
    line-height: 1.8;
    color: #4a5568;
    white-space: pre-wrap;
    background: #f7fafc;
    padding: 2rem;
    border-radius: 8px;
    border-left: 4px solid #667eea;
  }

  .empty-content {
    text-align: center;
    padding: 3rem;
    color: #a0aec0;
  }

  .empty-content .empty-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
    opacity: 0.5;
  }

  .empty-content p {
    font-size: 1.4rem;
    margin: 0;
  }

  .status-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
  }

  .status-item {
    display: flex;
    gap: 1.5rem;
    align-items: center;
  }

  .status-indicator {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  .status-complete { background: #10b981; }
  .status-pending { background: #f59e0b; }
  .status-warning { background: #ef4444; }

  .status-content {
    flex: 1;
  }

  .status-title {
    font-size: 1.2rem;
    color: #718096;
    font-weight: 600;
    margin-bottom: 0.3rem;
  }

  .status-value {
    font-size: 1.5rem;
    color: #2d3748;
    font-weight: 600;
  }

  .action-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 2px solid #e2e8f0;
  }

  .action-left,
  .action-right {
    display: flex;
    gap: 1rem;
  }

  .btn {
    padding: 1.2rem 2rem;
    font-size: 1.4rem;
    font-weight: 600;
    border-radius: 0.8rem;
    cursor: pointer;
    transition: all 0.3s;
    display: inline-flex;
    align-items: center;
    gap: 0.8rem;
    text-decoration: none;
    border: none;
  }

  .btn-icon {
    font-size: 1.6rem;
  }

  .btn-primary {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
  }

  .btn-secondary {
    background: #f3f4f6;
    color: #374151;
    border: 2px solid #e5e7eb;
  }

  .btn-secondary:hover {
    background: #e5e7eb;
  }

  .btn-warning {
    background: #fbbf24;
    color: #78350f;
  }

  .btn-warning:hover {
    background: #f59e0b;
    transform: translateY(-2px);
  }

  .btn-danger {
    background: #ef4444;
    color: white;
  }

  .btn-danger:hover {
    background: #dc2626;
    transform: translateY(-2px);
  }

  .empty-state-full {
    min-height: 50vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .empty-state-content {
    text-align: center;
    padding: 4rem 2rem;
    background: white;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    max-width: 600px;
  }

  .empty-icon-large {
    font-size: 8rem;
    margin-bottom: 2rem;
    opacity: 0.5;
  }

  .empty-state-content h2 {
    color: #2d3748;
    margin-bottom: 1rem;
    font-size: 2.4rem;
  }

  .empty-state-content p {
    color: #718096;
    font-size: 1.5rem;
    margin-bottom: 3rem;
    line-height: 1.6;
  }

  .empty-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (max-width: 768px) {
    .page-header-extended {
      flex-direction: column;
      align-items: flex-start;
      gap: 1.5rem;
    }

    .summary-cards {
      grid-template-columns: 1fr;
    }

    .two-column-layout {
      grid-template-columns: 1fr;
    }

    .action-bar {
      flex-direction: column;
    }

    .action-left,
    .action-right {
      width: 100%;
      flex-direction: column;
    }

    .btn {
      width: 100%;
      justify-content: center;
    }
  }
</style>

<script>
  function editAnalysis(id) {
    // TODO: Düzenleme sayfası oluşturulacak
    alert('Düzenleme özelliği yakında eklenecek!');
  }

  async function deleteAnalysis(id) {
    const confirmMessage = 'Bu analizi silmek istediğinizden emin misiniz?\n\nBu işlem geri alınamaz!';
    
    if (!confirm(confirmMessage)) {
      return;
    }

    try {
      const response = await fetch(`/analyses/${id}`, {
        method: 'DELETE',
        headers: { 'Content-Type': 'application/json' }
      });

      if (response.ok || response.status === 204) {
        alert('✅ Analiz başarıyla silindi!');
        window.location.href = '/analyses/dashboard';
      } else {
        const data = await response.json();
        throw new Error(data.message || 'Analiz silinemedi');
      }
    } catch (error) {
      console.error('Silme hatası:', error);
      alert('❌ Bir hata oluştu: ' + error.message);
    }
  }
</script>

<%- include('../includes/dashboard-footer') %>
